// diretorios.

// variaveis

try {
    // Variaveis CHAVE do localStorage
    // Crud - Read.
    // Crud - Update.
    // Crud - Delete.
    // classes DOM
} catch (error) {
    console.log("deu erro pois: \n", error);
}

try {
    window.addEventListener("load", function () {});
} catch (error) {
    console.log(" \n A pagina não tem h1 - nada.");
}
